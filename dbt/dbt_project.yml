name: advdatafinal
version: 0.1.0
config-version: 2

profile: advdatafinal

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - target
  - dbt_packages

models:
  advdatafinal:
    datos_masked:
      +schema: datos_masked
      +materialized: view
    silver:
      +schema: silver
      +materialized: table
    gold:
      +schema: gold
      +materialized: table
